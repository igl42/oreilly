/**************************************************************************************************
*
* \file ArraySize.cpp
* \brief C++ Training - Function Template Programming Task
*
* Copyright (C) 2015-2024 Klaus Iglberger - All Rights Reserved
*
* This file is part of the C++ training by Klaus Iglberger. The file may only be used in the
* context of the C++ training or with explicit agreement by Klaus Iglberger.
*
* Task: Write a 'constexpr' function template 'size()' that returns the size of a given array.
*
**************************************************************************************************/

#include <cstdlib>
#include <iostream>
#include <string>


// TODO: Write a 'constexpr' function template 'size()' that returns the size of a given array.


int main()
{
   /*
   int         array1[4];
   double      array2[6];
   std::string array3[9];

   constexpr size_t size1( size( array1 ) );
   constexpr size_t size2( size( array2 ) );
   constexpr size_t size3( size( array3 ) );

   if( size1 != 4UL || size2 != 6UL || size3 != 9UL )
   {
      std::cerr << "Error during 'size()' evaluation detected!\n";
   }
   else
   {
      std::cout << "size() works!\n";
   }
   */

   return EXIT_SUCCESS;
}
